{
  "name": "effectTextParser",
  "version": "1.0.0",
  "description": "Effect text parser for qpRemake, using XML",
  "main": "./dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Blu",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.13.1"
  },
  "dependencies": {
    "@rgrove/parse-xml": "^4.2.0"
  }
}